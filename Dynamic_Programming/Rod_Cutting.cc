// Guntas Singh
// Thapar University
#include <bits/stdc++.h>
#include<boost/multiprecision/cpp_int.hpp>
using namespace std;
using boost::multiprecision::cpp_int;


// Question:- Given a rod of length N and all the profits that can be made if we cut th rod from length 1 to N we have
// to cut the rod in such a way that it maximizes the profit.

// eg:- if we have the rod of length 4 and profits for length 1,2,3,4 are 2,3,2,5 respectively we can cut the rod in pieces of
// 1+1+1+1 with the profit 2+2+2+2=8 to have the max profit.!

// Time Complexity-->O(N^N);
int RecursiveApproach(int N,int profits[]){
	
}




int main(){
ios_base::sync_with_stdio(false);
cin.tie(NULL);

}